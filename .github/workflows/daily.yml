name: LinuxDo Multi-Site Automation

on:
  workflow_dispatch:
    inputs:
      site_selector:
        description: '选择要运行的站点'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - linux_do
          - idcflare
  schedule:
    - cron: '0 * * * *'

env:
  PYTHON_VERSION: '3.11'
  # 缓存Cookies的目录（与脚本中一致）
  COOKIES_CACHE_DIR: ${{ github.workspace }}

jobs:
  automate:
    runs-on: ubuntu-22.04
    timeout-minutes: 45

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: 恢复Cloudflare Cookies缓存
      uses: actions/cache@v3
      id: cookies-cache
      with:
        path: |
          cf_cookies_linux_do.json
          cf_cookies_idcflare.json
        key: ${{ runner.os }}-linuxdo-cookies-${{ github.sha }}
        # 允许使用旧缓存（即使代码变动也可复用Cookies）
        restore-keys: |
          ${{ runner.os }}-linuxdo-cookies-

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install system dependencies for Chromium
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libnss3 \
          libatk-bridge2.0-0 \
          libdrm2 \
          libxkbcommon0 \
          libxcomposite1 \
          libxdamage1 \
          libxrandr2 \
          libgbm1 \
          libxss1 \
          libasound2 \
          libatspi2.0-0 \
          libcups2 \
          libx11-xcb1

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip list

    - name: Create turnstilePatch extension
      run: |
        echo "🔧 创建turnstilePatch扩展..."
        python setup_directories.py
        echo "📁 检查扩展目录:"
        ls -la turnstilePatch/

    - name: Run automation script
      env:
        LINUXDO_USERNAME: ${{ secrets.LINUXDO_USERNAME }}
        LINUXDO_PASSWORD: ${{ secrets.LINUXDO_PASSWORD }}
        IDCFLARE_USERNAME: ${{ secrets.IDCFLARE_USERNAME }}
        IDCFLARE_PASSWORD: ${{ secrets.IDCFLARE_PASSWORD }}
        GITHUB_ACTIONS: 'true'
        DOH_SERVER: 'https://ld.ddd.oaifree.com/query-dns'
        # 传递站点选择参数到脚本
        SITE_SELECTOR: ${{ github.event.inputs.site_selector || 'all' }}
      run: |
        echo "开始执行自动化脚本..."
        echo "站点选择: $SITE_SELECTOR"
        echo "当前时间: $(date)"
        echo "DoH服务器: $DOH_SERVER"
        echo "Cookies缓存状态: ${{ steps.cookies-cache.outputs.cache-hit == 'true' && '已命中' || '未命中' }}"
        python main.py
        echo "脚本执行完成"

    - name: 显示缓存的Cookies文件（调试用）
      run: |
        echo "📋 当前目录下的Cookies文件:"
        ls -la cf_cookies_*.json 2>/dev/null || echo "无Cookies文件"

    - name: Upload session data（备份用）
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: linuxdo-session-data
        path: |
          cf_cookies_linux_do.json
          cf_cookies_idcflare.json
          browser_state_linux_do.json
          browser_state_idcflare.json
        retention-days: 7
        overwrite: true

    - name: Upload debug files
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: connect-debug-files
        path: |
          connect_debug_*.html
          connect_debug_*.png
          login_debug_*.html
          login_error_*.html
        retention-days: 7

    - name: Notify on failure
      if: failure()
      uses: actions/github-script@v6
      with:
        script: |
          const { owner, repo } = context.repo;
          github.rest.issues.create({
            owner,
            repo,
            title: 'LinuxDo 自动化执行失败',
            body: `LinuxDo 多站点自动化脚本执行失败，请检查日志。
            
            工作流运行: ${context.runId}
            触发时间: ${new Date().toISOString()}
            
            请查看 Action 日志获取详细错误信息。`
          });
