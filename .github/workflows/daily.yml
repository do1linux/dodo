name: LinuxDo Multi-Site Automation

on:
  workflow_dispatch:
    inputs:
      site_selector:
        description: '选择要运行的站点'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - linux_do
          - idcflare
  schedule:
    - cron: '0 * * * *' 

env:
  PYTHON_VERSION: '3.11'

jobs:
  automate:
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libnss3 \
          libatk-bridge2.0-0 \
          libdrm2 \
          libxkbcommon0 \
          libxcomposite1 \
          libxdamage1 \
          libxrandr2 \
          libgbm1 \
          libxss1 \
          libasound2 \
          libatspi2.0-0 \
          libcups2 \
          libx11-xcb1 \
          wget \
          unzip

    - name: Install Chrome
      run: |
        # 安装 Google Chrome
        wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
        echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable
        echo "Chrome版本: $(google-chrome --version)"

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip list

    - name: Download previous session data
      uses: actions/download-artifact@v4
      with:
        name: linuxdo-session-data
        path: cache/
      continue-on-error: true

    - name: Display cache status
      run: |
        echo "📦 缓存文件状态:"
        echo "缓存目录内容:"
        ls -la cache/ 2>/dev/null || echo "缓存目录不存在"
        echo ""
        echo "各站点缓存文件:"
        for site in linux_do idcflare; do
          echo "=== $site ==="
          ls -la cache/${site}_cookies.json 2>/dev/null || echo "  - ${site}_cookies.json: 不存在"
          ls -la cache/browser_state_${site}.json 2>/dev/null || echo "  - browser_state_${site}.json: 不存在"
        done

    - name: Create cache directory
      run: |
        mkdir -p cache
        echo "📁 创建缓存目录"

    - name: Run automation script
      env:
        LINUXDO_USERNAME: ${{ secrets.LINUXDO_USERNAME }}
        LINUXDO_PASSWORD: ${{ secrets.LINUXDO_PASSWORD }}
        IDCFLARE_USERNAME: ${{ secrets.IDCFLARE_USERNAME }}
        IDCFLARE_PASSWORD: ${{ secrets.IDCFLARE_PASSWORD }}
        GITHUB_ACTIONS: 'true'
        BROWSE_ENABLED: 'true'
        HEADLESS: 'true'
      run: |
        echo "开始执行自动化脚本..."
        echo "站点选择: ${{ github.event.inputs.site_selector || 'all' }}"
        echo "当前时间: $(date)"
        echo "缓存检查:"
        python -c "
        import os, json
        from datetime import datetime
        sites = ['linux_do', 'idcflare']
        for site in sites:
            cookie_file = f'cache/{site}_cookies.json'
            state_file = f'cache/browser_state_{site}.json'
            cookie_exists = os.path.exists(cookie_file)
            state_exists = os.path.exists(state_file)
            
            if cookie_exists:
                try:
                    with open(cookie_file, 'r') as f:
                        data = json.load(f)
                    cache_time = data.get('cache_time', '')
                    if cache_time:
                        cache_dt = datetime.fromisoformat(cache_time)
                        now = datetime.now()
                        age = (now - cache_dt).days
                        print(f'{site}: cookies=存在 (已缓存{age}天), browser_state={state_exists}')
                    else:
                        print(f'{site}: cookies=存在 (无时间信息), browser_state={state_exists}')
                except Exception as e:
                    print(f'{site}: cookies=损坏 ({e}), browser_state={state_exists}')
            else:
                print(f'{site}: cookies=不存在, browser_state={state_exists}')
        "
        python main.py
        echo "脚本执行完成"

    - name: Generate browser state files
      run: |
        echo "📝 生成浏览器状态文件..."
        current_time=$(date -Iseconds)
        for site in linux_do idcflare; do
          state_file="cache/browser_state_${site}.json"
          cat > "$state_file" << EOF
{
  "site": "${site}",
  "last_updated": "${current_time}",
  "status": "completed",
  "version": "1.0",
  "browse_count": 5,
  "login_success": true
}
EOF
          echo "生成: $state_file"
        done

    - name: Upload session data
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: linuxdo-session-data
        path: |
          cache/linux_do_cookies.json
          cache/idcflare_cookies.json
          cache/browser_state_linux_do.json
          cache/browser_state_idcflare.json
        retention-days: 7
        overwrite: true

    - name: Display uploaded artifacts
      run: |
        echo "📤 上传的缓存文件:"
        echo "缓存目录内容:"
        ls -la cache/ 2>/dev/null || echo "缓存目录不存在"
        echo ""
        echo "各站点文件详情:"
        for site in linux_do idcflare; do
          echo "=== $site ==="
          cookie_file="cache/${site}_cookies.json"
          state_file="cache/browser_state_${site}.json"
          
          if [ -f "$cookie_file" ]; then
            size=$(stat -c%s "$cookie_file" 2>/dev/null || echo "未知")
            echo "  - ${site}_cookies.json: ${size} 字节"
          else
            echo "  - ${site}_cookies.json: 不存在"
          fi
          
          if [ -f "$state_file" ]; then
            size=$(stat -c%s "$state_file" 2>/dev/null || echo "未知")
            echo "  - browser_state_${site}.json: ${size} 字节"
          else
            echo "  - browser_state_${site}.json: 不存在"
          fi
        done

    - name: Notify on failure
      if: failure()
      uses: actions/github-script@v6
      with:
        script: |
          const { owner, repo } = context.repo;
          github.rest.issues.create({
            owner,
            repo,
            title: 'LinuxDo 自动化执行失败',
            body: `LinuxDo 多站点自动化脚本执行失败，请检查日志。
            
            工作流运行: ${context.runId}
            触发时间: ${new Date().toISOString()}
            
            请查看 Action 日志获取详细错误信息。`
          });
