name: LinuxDo Multi-Site Automation

on:
  workflow_dispatch:
    inputs:
      site_selector:
        description: '选择要运行的站点'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - linux_do
          - idcflare
  schedule:
    - cron: '0 * * * *'

env:
  PYTHON_VERSION: '3.11'

jobs:
  automate:
    runs-on: ubuntu-22.04
    timeout-minutes: 45

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install system dependencies for Chromium
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libnss3 \
          libatk-bridge2.0-0 \
          libdrm2 \
          libxkbcommon0 \
          libxcomposite1 \
          libxdamage1 \
          libxrandr2 \
          libgbm1 \
          libxss1 \
          libasound2 \
          libatspi2.0-0 \
          libcups2 \
          libx11-xcb1

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip list

    - name: Download previous session data
      uses: actions/download-artifact@v4
      with:
        name: linuxdo-session-data
        path: ./
      continue-on-error: true

    - name: Display cache status
      run: |
        echo "📦 缓存文件状态:"
        echo "cf_cookies 文件:"
        ls -la cf_cookies_*.json 2>/dev/null || echo "未找到 cf_cookies 文件"
        echo ""
        echo "browser_state 文件:"
        ls -la browser_state_*.json 2>/dev/null || echo "未找到 browser_state 文件"
        echo ""

    - name: Create turnstilePatch extension
      run: |
        echo "🔧 创建turnstilePatch扩展..."
        python setup_directories.py
        echo "📁 检查扩展目录:"
        ls -la turnstilePatch/

    - name: Run automation script
      env:
        LINUXDO_USERNAME: ${{ secrets.LINUXDO_USERNAME }}
        LINUXDO_PASSWORD: ${{ secrets.LINUXDO_PASSWORD }}
        IDCFLARE_USERNAME: ${{ secrets.IDCFLARE_USERNAME }}
        IDCFLARE_PASSWORD: ${{ secrets.IDCFLARE_PASSWORD }}
        GITHUB_ACTIONS: 'true'
        DOH_SERVER: 'https://ld.ddd.oaifree.com/query-dns'
      run: |
        echo "开始执行自动化脚本..."
        echo "站点选择: ${{ github.event.inputs.site_selector || 'all' }}"
        echo "当前时间: $(date)"
        echo "DoH服务器: $DOH_SERVER"
        echo "缓存检查:"
        python -c "
        import os, json
        sites = ['linux_do', 'idcflare']
        for site in sites:
            cf_file = f'cf_cookies_{site}.json'
            state_file = f'browser_state_{site}.json'
            cf_exists = os.path.exists(cf_file)
            state_exists = os.path.exists(state_file)
            print(f'{site}: cf_cookies={cf_exists}, browser_state={state_exists}')
        "
        python main.py
        echo "脚本执行完成"

    - name: Upload session data
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: linuxdo-session-data
        path: |
          cf_cookies_linux_do.json
          cf_cookies_idcflare.json
          browser_state_linux_do.json
          browser_state_idcflare.json
        retention-days: 7
        overwrite: true

    - name: Display uploaded artifacts
      run: |
        echo "📤 上传的缓存文件:"
        echo "cf_cookies 文件:"
        ls -la cf_cookies_*.json 2>/dev/null || echo "未生成 cf_cookies 文件"
        echo ""
        echo "browser_state 文件:"
        ls -la browser_state_*.json 2>/dev/null || echo "未生成 browser_state 文件"

    - name: Upload debug files
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: connect-debug-files
        path: |
          connect_debug_*.html
          connect_debug_*.png
          login_debug_*.html
          login_error_*.html
        retention-days: 7

    - name: Notify on failure
      if: failure()
      uses: actions/github-script@v6
      with:
        script: |
          const { owner, repo } = context.repo;
          github.rest.issues.create({
            owner,
            repo,
            title: 'LinuxDo 自动化执行失败',
            body: `LinuxDo 多站点自动化脚本执行失败，请检查日志。
            
            工作流运行: ${context.runId}
            触发时间: ${new Date().toISOString()}
            
            请查看 Action 日志获取详细错误信息。`
          });
